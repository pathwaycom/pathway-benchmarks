# ENGINE_TYPE=pathway RATE_PER_SECOND=300000 docker-compose -p $USER --env-file variables_ext.env -f docker-compose-pathway.yml up stats-collector> pathway_log.txt>&1 --build
# docker-compose -p $USER down --remove-orphans

# ENGINE_TYPE=kstreams RATE_PER_SECOND=300000 docker-compose -p $USER --env-file variables_ext.env -f docker-compose-kafka-streams.yml up stats-collector> kstreams_log.txt>&1 --build
# docker-compose -p $USER down --remove-orphans

# ENGINE_TYPE=flink RATE_PER_SECOND=300000 docker-compose -p $USER --env-file variables_ext.env -f docker-compose-flink-word-count.yml up stats-collector> flink_log.txt>&1 --build
# docker-compose -p $USER down --remove-orphans

# ENGINE_TYPE=spark RATE_PER_SECOND=300000 docker-compose -p $USER --env-file variables_ext.env -f docker-compose-spark-word-count.yml up stats-collector> spark_log.txt>&1 --build
# docker-compose -p $USER down --remove-orphans


# ENGINE_TYPE=flink RATE_PER_SECOND=50000 docker-compose -p $USER --env-file variables_ext.env -f docker-compose-flink-word-count.yml up stats-collector> flink_log.txt>&1 --build
# docker-compose -p $USER down --remove-orphans

# ENGINE_TYPE=flink RATE_PER_SECOND=75000 docker-compose -p $USER --env-file variables_ext.env -f docker-compose-flink-word-count.yml up stats-collector> flink_log.txt>&1 --build
# docker-compose -p $USER down --remove-orphans

# ENGINE_TYPE=flink RATE_PER_SECOND=100000 docker-compose -p $USER --env-file variables_ext.env -f docker-compose-flink-word-count.yml up stats-collector> flink_log.txt>&1 --build
# docker-compose -p $USER down --remove-orphans

# ENGINE_TYPE=flink RATE_PER_SECOND=125000 docker-compose -p $USER --env-file variables_ext.env -f docker-compose-flink-word-count.yml up stats-collector> flink_log.txt>&1 --build
# docker-compose -p $USER down --remove-orphans

# ENGINE_TYPE=flink RATE_PER_SECOND=150000 docker-compose -p $USER --env-file variables_ext.env -f docker-compose-flink-word-count.yml up stats-collector> flink_log.txt>&1 --build
# docker-compose -p $USER down --remove-orphans

# ENGINE_TYPE=flink RATE_PER_SECOND=175000 docker-compose -p $USER --env-file variables_ext.env -f docker-compose-flink-word-count.yml up stats-collector> flink_log.txt>&1 --build
# docker-compose -p $USER down --remove-orphans

# ENGINE_TYPE=flink RATE_PER_SECOND=200000 docker-compose -p $USER --env-file variables_ext.env -f docker-compose-flink-word-count.yml up stats-collector> flink_log.txt>&1 --build
# docker-compose -p $USER down --remove-orphans

# ENGINE_TYPE=flink RATE_PER_SECOND=225000 docker-compose -p $USER --env-file variables_ext.env -f docker-compose-flink-word-count.yml up stats-collector> flink_log.txt>&1 --build
# docker-compose -p $USER down --remove-orphans

# ENGINE_TYPE=flink RATE_PER_SECOND=250000 docker-compose -p $USER --env-file variables_ext.env -f docker-compose-flink-word-count.yml up stats-collector> flink_log.txt>&1 --build
# docker-compose -p $USER down --remove-orphans

# ENGINE_TYPE=flink RATE_PER_SECOND=275000 docker-compose -p $USER --env-file variables_ext.env -f docker-compose-flink-word-count.yml up stats-collector> flink_log.txt>&1 --build
# docker-compose -p $USER down --remove-orphans

# ENGINE_TYPE=flink RATE_PER_SECOND=300000 docker-compose -p $USER --env-file variables_ext.env -f docker-compose-flink-word-count.yml up stats-collector> flink_log.txt>&1 --build
# docker-compose -p $USER down --remove-orphans

# ENGINE_TYPE=pathway RATE_PER_SECOND=50000 docker-compose -p $USER --env-file variables_ext.env -f docker-compose-pathway.yml up stats-collector> pathway_log01.txt>&1 --build
# docker-compose -p $USER down --remove-orphans

# ENGINE_TYPE=pathway RATE_PER_SECOND=75000 docker-compose -p $USER --env-file variables_ext.env -f docker-compose-pathway.yml up stats-collector> pathway_log02.txt>&1 --build
# docker-compose -p $USER down --remove-orphans

# ENGINE_TYPE=pathway RATE_PER_SECOND=100000 docker-compose -p $USER --env-file variables_ext.env -f docker-compose-pathway.yml up stats-collector> pathway_log03.txt>&1 --build
# docker-compose -p $USER down --remove-orphans

# ENGINE_TYPE=pathway RATE_PER_SECOND=125000 docker-compose -p $USER --env-file variables_ext.env -f docker-compose-pathway.yml up stats-collector> pathway_log04.txt>&1 --build
# docker-compose -p $USER down --remove-orphans

# ENGINE_TYPE=pathway RATE_PER_SECOND=150000 docker-compose -p $USER --env-file variables_ext.env -f docker-compose-pathway.yml up stats-collector> pathway_log05.txt>&1 --build
# docker-compose -p $USER down --remove-orphans

# ENGINE_TYPE=pathway RATE_PER_SECOND=175000 docker-compose -p $USER --env-file variables_ext.env -f docker-compose-pathway.yml up stats-collector> pathway_log06.txt>&1 --build
# docker-compose -p $USER down --remove-orphans

# ENGINE_TYPE=pathway RATE_PER_SECOND=200000 docker-compose -p $USER --env-file variables_ext.env -f docker-compose-pathway.yml up stats-collector> pathway_log07.txt>&1 --build
# docker-compose -p $USER down --remove-orphans

# ENGINE_TYPE=pathway RATE_PER_SECOND=225000 docker-compose -p $USER --env-file variables_ext.env -f docker-compose-pathway.yml up stats-collector> pathway_log08.txt>&1 --build
# docker-compose -p $USER down --remove-orphans

# ENGINE_TYPE=pathway RATE_PER_SECOND=250000 docker-compose -p $USER --env-file variables_ext.env -f docker-compose-pathway.yml up stats-collector> pathway_log09.txt>&1 --build
# docker-compose -p $USER down --remove-orphans

# ENGINE_TYPE=pathway RATE_PER_SECOND=275000 docker-compose -p $USER --env-file variables_ext.env -f docker-compose-pathway.yml up stats-collector> pathway_log10.txt>&1 --build
# docker-compose -p $USER down --remove-orphans

# ENGINE_TYPE=pathway RATE_PER_SECOND=300000 docker-compose -p $USER --env-file variables_ext.env -f docker-compose-pathway.yml up stats-collector> pathway_log11.txt>&1 --build
# docker-compose -p $USER down --remove-orphans


ENGINE_TYPE=flink RATE_PER_SECOND=250000 docker-compose -p $USER --env-file variables_ext.env -f docker-compose-flink-word-count.yml up stats-collector> flink_log.txt>&1 --build
docker-compose -p $USER down --remove-orphans

ENGINE_TYPE=flink RATE_PER_SECOND=275000 docker-compose -p $USER --env-file variables_ext.env -f docker-compose-flink-word-count.yml up stats-collector> flink_log.txt>&1 --build
docker-compose -p $USER down --remove-orphans

ENGINE_TYPE=pathway RATE_PER_SECOND=250000 docker-compose -p $USER --env-file variables_ext.env -f docker-compose-pathway.yml up stats-collector> pathway_log09.txt>&1 --build
docker-compose -p $USER down --remove-orphans

ENGINE_TYPE=pathway RATE_PER_SECOND=275000 docker-compose -p $USER --env-file variables_ext.env -f docker-compose-pathway.yml up stats-collector> pathway_log10.txt>&1 --build
docker-compose -p $USER down --remove-orphans

